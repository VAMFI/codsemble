interface:
  display_name: "Rollback Codex Team"
  short_description: "Safely revert an owned team transaction"
  default_prompt: "Use $rollback-team to preview a locked no-clobber rollback for this workspace."
